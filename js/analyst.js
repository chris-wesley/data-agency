/* Analyst level */

// Initial game variables
let level = "Analyst";
let upcomingLevel = "Manager";
currentLevel(level);

startingScore(5000);

populateDashboard(3, 6, 2, 2);

startLevel();

createMessage(
	"manager",
	"explaining",
	"Manager",
	"No one’s going to be holding your hand anymore. You’re in charge of important clients, make the agency the biggest profit."
);