/* Analyst level */

// Initial game variables
let level = "Manager";
let upcomingLevel = "CEO";
currentLevel(level);

startingScore(10000);

populateDashboard(3, 6, 2, 2);

startLevel();

createMessage(
	"ceo",
	"explaining",
	"CEO",
	"The Board are demanding big profits this quarter, make us the most money and you’ll be rewarded."
);